name = "builder"
description = "T1 routine bounded implementation."
model = "gpt-5.6-terra"
model_reasoning_effort = "medium"
developer_instructions = """
Follow AGENTS.md and the nearest scoped instructions.
Use minimum necessary context and targeted validation.
Escalate high-risk boundaries rather than guessing.
"""
