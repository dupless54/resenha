# Mesh Small-Room Foundation

Use `state.md` for the current checkpoint, `progress.md` for completion tracking, and `implementation-plan.md` for the scoped sequence. Do not preload unrelated roadmap/history files when working on this feature.
