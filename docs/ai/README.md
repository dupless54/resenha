# AI context system
`CURRENT_STATE.md` is the short checkpoint; `WORKFLOW.md` owns cross-model roles. For genuine multi-session work add only `docs/ai/work/<feature>/{state.md,progress.md,implementation-plan.md}` unless separate phase detail is necessary. Store verified facts, not chat transcripts.
